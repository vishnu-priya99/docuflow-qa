fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# Database
sqlalchemy>=2.0.35
asyncpg>=0.29.0
greenlet>=3.0.0

# Vector store
qdrant-client>=1.11.0

# LLM/embeddings: only "ollama" and "mock"/"local" are implemented
# (see LLM_PROVIDER/EMBEDDING_PROVIDER) - both need no SDK at all, just
# plain HTTP via httpx (already a transitive dependency).

# Orchestration
langgraph>=0.2.40
langchain-core>=0.3.15
langchain-text-splitters>=0.3.0

# Document parsing
pdfplumber>=0.11.0
python-docx>=1.1.2
python-pptx>=1.0.2
openpyxl>=3.1.5
pandas>=2.2.0

# SQL parsing / safety
sqlglot>=25.24.0

# Reranking (only needed if RERANK_PROVIDER=cross_encoder - pulls in
# torch). Comment out if RERANK_PROVIDER=llm.
sentence-transformers>=3.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
fpdf2>=2.7.0
